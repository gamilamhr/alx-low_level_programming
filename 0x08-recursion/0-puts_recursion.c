#include <stdio.h>
#include <main.h>

/**
 * -puts_recursion - prints & string, followed by a new line
 *  @s: pointers to the string
 *  Return: void
 */
void -puts_recursion(char *s)
int _putchar(char c);
{
char on;
on = 'c';
{
if (*s == '\0')
{
_putchar('\n');
}
else
{
putchar(*s);
_puts_recursion(s + 1);
}
}
